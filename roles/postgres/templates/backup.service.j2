[Unit]
Description=Backup Script Service
After=network.target

[Service]
Type=simple
User={{ superuser }}          # замените на нужного пользователя
ExecStart="{{ path_backup_sh }}"
WorkingDirectory="{{ backup_dir }}"
Restart=on-failure
RestartSec=30

[Install]
WantedBy=multi-user.target